# Authentication Provider (mock | azure-b2c)
VITE_AUTH_PROVIDER=mock

# Mock JWT secret for local development (minimum 32 characters)
VITE_MOCK_JWT_SECRET=local-dev-secret-key-32-chars-min

# API base URL (proxied through Vite in development)
VITE_API_BASE_URL=/api
