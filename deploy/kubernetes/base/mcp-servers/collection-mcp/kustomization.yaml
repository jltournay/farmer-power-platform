apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: collection-mcp
  app.kubernetes.io/component: mcp-server
  app.kubernetes.io/part-of: farmer-power-platform
