apiVersion: v1
kind: Service
metadata:
  name: plantation-mcp
  labels:
    app: plantation-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app: plantation-mcp
