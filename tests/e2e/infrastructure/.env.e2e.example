# E2E Test Environment Variables
# ================================
# Copy this file to .env.e2e and fill in real values:
#   cp .env.e2e.example .env.e2e
#
# IMPORTANT: Never commit .env.e2e - it contains secrets!
#
# For CI: These are injected from GitHub Secrets in e2e-tests.yaml

# =============================================================================
# OpenRouter (LLM Gateway)
# =============================================================================
# Required for: AI Model service (Story 0.75.5+)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# =============================================================================
# Azure Document Intelligence (PDF OCR)
# =============================================================================
# Required for: RAG document extraction (Story 0.75.10c)
# Create resource at: https://portal.azure.com -> Document Intelligence
AZURE_DOCUMENT_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DOCUMENT_KEY=your-azure-key-here

# =============================================================================
# Pinecone (Vector Database) - Serverless / Free Tier
# =============================================================================
# Required for: RAG vector storage (Stories 0.75.12-15)
# Get your key at: https://app.pinecone.io -> API Keys
#
# Note: Pinecone serverless (free tier) doesn't use ENVIRONMENT.
# The host is determined automatically from your index.
# Find your index host at: Pinecone Console -> Your Index -> Host
PINECONE_API_KEY=your-pinecone-key-here
PINECONE_INDEX_HOST=https://your-index-xxxxxxx.svc.aped-xxxx-xxxx.pinecone.io

# =============================================================================
# Optional: Cost Control
# =============================================================================
# Set budget limits for E2E test runs (prevents runaway costs)
# OPENROUTER_DAILY_BUDGET_USD=5.00
# OPENROUTER_PER_REQUEST_MAX_USD=0.50
