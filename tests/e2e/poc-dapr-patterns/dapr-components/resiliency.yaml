apiVersion: dapr.io/v1alpha1
kind: Resiliency
metadata:
  name: poc-resiliency
spec:
  policies:
    retries:
      pubsubRetry:
        policy: constant
        duration: 1s
        maxRetries: 3
  targets:
    components:
      pubsub:
        inbound:
          retry: pubsubRetry
